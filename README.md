This repository contains some usefull python functions. 
To use these functions download the repository and add the path of the
directory to the PYTHONPATH, then you can import akslib and use the
functions inside it.
